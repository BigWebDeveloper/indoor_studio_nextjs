import { IconType } from "react-icons";
export type shareLinkType = {
  Icon: IconType;
  title: string;
  url: string;
};

export type songDataType = {
  artistName: string;
  title: string;
  lyrics: string;
  description: string;
};
